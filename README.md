## How to Run Locally

This is a static single-page application (SPA). To run it locally and avoid CORS issues, use a local server:

### Using Python

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`

### Using npx

```bash
npx serve .
```
